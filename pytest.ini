[pytest]
# Run from the repo root: `pytest`
# tests/conftest.py adds backend/ to sys.path so `import app` and
# `from services... import ...` resolve.
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra
