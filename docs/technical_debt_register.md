# Technical Debt Register

Updated: 2026-07-17

Severity reflects pilot risk, not long-term production importance. `blocking_pilot=true` means the item must be fixed before a classroom pilot can proceed.

| debt_id | title | description | affected_files | severity | likelihood | impact | recommended_action | suggested_task | estimated_scope | blocking_pilot |
|---|---|---|---|---|---|---|---|---|---|---|
| TD-001 | `backend/app.py` is too large | The Flask app still contains models, many routes, serialization, migrations, and glue logic in one large file. Mitigation is in progress: Task 9C.1 extracted teacher learning analytics routes into `backend/routes/teacher_learning_analytics.py`, Task 9C.2 extracted student Concept Card learning routes into `backend/routes/student_concept_cards.py`, Task 9C.2.1 added a minimal `backend/routes/shared.py` route foundation, Task 9C.3A extracted teacher Concept Card review routes into `backend/routes/concept_card_review.py`, Task 9C.3B extracted teacher-facing student feedback queue/triage routes into `backend/routes/concept_card_feedback.py`, Task 9C.4A extracted only read-only provider governance/preflight GET routes into `backend/routes/provider_governance.py`, Task 9C.4B extracted only provider policy mutation into `backend/routes/provider_policy.py`, and Task 9C.4C extracted only provider preflight execution into `backend/routes/provider_preflight.py`. Task 9C.4D characterized `POST /api/alignment/verify` and concluded `SERVICE_BOUNDARY_REQUIRED_FIRST`; Task 9C.4D.1 established `backend/services/alignment_verification_execution.py`; Task 9C.4E extracted the resulting thin HTTP adapter into `backend/routes/alignment_verification.py`; Task 9C.4F inventoried remaining provider/admin routes; Task 9C.4G extracted only `GET /api/admin/alignment-runs` into `backend/routes/admin_alignment_runs.py`; Task 9C.4H characterized legacy `/api/admin/ai/*` and concluded `SPLIT_READONLY_AND_HEALTHCHECK_FIRST`; Task 9C.4I extracted only the legacy provider admin observability GET views into `backend/routes/legacy_provider_admin_observability.py`; Task 9C.4J established `backend/services/legacy_provider_registry_seed.py` so legacy provider registry seed and flush behavior is shared outside route handlers while callers keep transaction ownership; Task 9C.4K extracted only the seed-backed legacy provider admin configuration GET views into `backend/routes/legacy_provider_admin_configuration.py`; Task 9C.4L characterized `POST /api/admin/ai/healthcheck`; Task 9C.4L.1 disabled the legacy live-probe transport path; Task 9C.4M established `backend/services/legacy_provider_local_readiness.py`; Task 9C.4N extracted the thin legacy healthcheck route into `backend/routes/legacy_provider_admin_healthcheck.py`; Task 9C.4O characterized prompt mutation and concluded `PROMPT_VERSIONING_OR_CONCURRENCY_POLICY_REQUIRED_FIRST`. This is not fully resolved. | `backend/app.py`, `backend/services/alignment_verification_execution.py`, `backend/services/legacy_provider_registry_seed.py`, `backend/services/legacy_provider_local_readiness.py`, `backend/routes/shared.py`, `backend/routes/teacher_learning_analytics.py`, `backend/routes/student_concept_cards.py`, `backend/routes/concept_card_review.py`, `backend/routes/concept_card_feedback.py`, `backend/routes/provider_governance.py`, `backend/routes/provider_policy.py`, `backend/routes/provider_preflight.py`, `backend/routes/alignment_verification.py`, `backend/routes/admin_alignment_runs.py`, `backend/routes/legacy_provider_admin_observability.py`, `backend/routes/legacy_provider_admin_configuration.py`, `backend/routes/legacy_provider_admin_healthcheck.py`, `docs/alignment_verification_route_boundary.md`, `docs/provider_admin_route_inventory.md`, `docs/legacy_provider_admin_surface.md`, `docs/legacy_provider_healthcheck_boundary.md`, `docs/legacy_provider_prompt_mutation_boundary.md` | high | high | Slower maintenance, harder review, route coupling | Continue staged route extraction with contract/E2E gates before app factory work; define prompt mutation policy before service extraction, and keep legacy `/api/alignment/run` as a separate execution-boundary task | Backend modularization | large | false |
| TD-002 | Service/app route duplication | Some business rules exist in both route glue and service modules, especially around audit and error handling. Task 9C.2.1 reduced route registration parameter growth by centralizing common route infrastructure in `RouteCoreDependencies`, but RBAC, audit adapters, and domain-specific error handling remain fragmented. | `backend/app.py`, `backend/routes/shared.py`, `backend/routes/*`, `backend/services/*` | high | medium | Inconsistent behavior across APIs | Continue narrow route helper consolidation without building a global service locator | Route helper consolidation | medium | false |
| TD-003 | Legacy Term/TerminologyCard coexistence | Legacy terminology models coexist with ConceptAlignmentCard and can confuse reporting/exports. | `backend/app.py`, `backend/services/*`, frontend | medium | high | Data interpretation drift | Document model boundaries and plan migration/backfill | Legacy terminology migration | large | false |
| TD-004 | Old helper calls remain | Helpers such as legacy chunk indexing and old card generation still exist for older routes. | `backend/app.py`, legacy scripts | medium | medium | Unexpected legacy behavior during demos | Keep covered by regression tests; remove only after replacement workflow is default | Legacy helper retirement | medium | false |
| TD-005 | Feedback schema upgraded through additive columns | Feedback is backward-compatible but spans old pilot feedback and new Concept Card feedback. | `backend/app.py`, `backend/services/concept_card_feedback.py` | high | medium | Missing columns in old DB can break triage/analytics | Keep upgrade-path tests; consider dedicated ConceptCardFeedback table later | Feedback domain split | medium | false |
| TD-006 | SQLite concurrency boundary | SQLite is suitable for local/demo/small pilot, but concurrent class usage can hit write locks. | database config, `scripts/*` | high | medium | Failed writes under concurrent use | Use SQLite for small pilot only; move to Postgres for broader deployment | Production database migration | large | false |
| TD-007 | Flask dev server boundary | Current run scripts target local Flask development server, not hardened production serving. | `scripts/run_backend.sh`, README | high | high | Misuse in production | Document pilot-only boundary; add WSGI deployment plan later | Deployment hardening | medium | false |
| TD-008 | Browser E2E coverage is limited | Task 9B.1 installed Python Playwright/Chromium and the student/teacher browser flows now pass twice locally. Coverage is still limited to desktop Chromium and core pilot paths. | `scripts/run_browser_e2e.py`, `.github/workflows/browser-e2e.yml`, `frontend/index.html` | medium | medium | Mobile, cross-browser, accessibility, and visual regressions can still escape | Keep the browser E2E gate blocking for pilots; add mobile/cross-browser/visual coverage after pilot | Browser E2E coverage expansion | medium | false |
| TD-009 | Analytics real-time aggregation | Teacher/student analytics aggregate from live tables without caching/materialization. | `backend/services/teacher_learning_analytics.py`, `student_learning_progress.py` | medium | medium | Slow queries as data grows | Add indexes/materialized summaries after pilot data volume is known | Analytics performance pass | medium | false |
| TD-010 | Migration mechanism is additive, not Alembic | Current migration is `create_all` plus `ensure_schema_columns`, not versioned Alembic migrations. | `scripts/migrate_db.py`, `backend/app.py` | high | high | Harder rollback/backfill control | Keep pilot upgrade tests; plan Alembic before production | Migration system | large | false |
| TD-011 | OpenAPI drift risk | Many routes exist; parity is now checked for main chain only, not every legacy endpoint. | `docs/openapi.yaml`, `tests/test_openapi_route_parity.py` | medium | high | API docs can drift | Expand allowlist and add route tags incrementally | OpenAPI coverage expansion | medium | false |
| TD-012 | RBAC domains are fragmented | Auth roles, course review permissions, course membership, and provider policies are separate gates. | `backend/app.py`, `backend/services/course_review_policy.py`, `student_course_access.py` | high | medium | Harder authorization reasoning | Build a unified authorization service after pilot | RBAC consolidation | large | false |
| TD-013 | Demo credentials risk | Demo users are local-only but easy to confuse with real accounts. | `scripts/seed_review_demo.py`, README | medium | medium | Unsafe if reused outside demo | Keep demo namespace, document local-only, require reset/rotate for real pilots | Demo credential handling | small | false |
| TD-014 | Real alignment provider not implemented | Mock/fake/replay validate contracts only; no production LLM verification exists. | `backend/services/alignment_*`, provider docs | medium | high | Users may overread verification scores | Keep UI/documentation explicit; future provider task must reuse gates | Real provider integration | large | false |
| TD-015 | Mock/fake/replay boundary needs continued messaging | Provider output can be mistaken for final semantic truth. | frontend, README, docs | medium | medium | Trust misunderstanding | Maintain labels and tests for `is_production_result=false` and `can_auto_approve=false` | Trust labeling pass | small | false |
| TD-016 | Audit table growth | AuditRecord keeps growing and has no retention/archival policy. | `audit_record`, `backend/services/audit_records.py` | medium | medium | DB growth and slower audits | Define retention/export policy before larger rollout | Audit retention | medium | false |
| TD-017 | Evidence JSON/text schema is flexible | Evidence fields are JSON stored in text, with multiple shapes across draft/review/student flows. | `ConceptAlignmentCard`, services | medium | medium | Serialization drift | Introduce typed evidence schema/version field | Evidence schema hardening | medium | false |
| TD-018 | Evidence duplication | Evidence snapshots are stored in card JSON and also in chunks/sources. | `concept_alignment_card`, knowledge tables | medium | medium | Storage growth and stale snapshots | Store bounded snapshots and source UIDs; consider normalized evidence link table | Evidence normalization | medium | false |
| TD-019 | Backup/restore remains script-level operations | Task 9B added backup, verification, restore, and automated tests. The app still does not enforce retention, scheduling, or offsite backup policy. | `scripts/pilot_backup.py`, `scripts/verify_pilot_backup.py`, `scripts/pilot_restore.py`, docs | medium | medium | Recovery depends on operator discipline | Use the runbook for every pilot; add scheduled/offsite backups before production | Backup operations policy | medium | false |
| TD-020 | SQLAlchemy legacy warnings | Current app still uses patterns that can trigger SQLAlchemy deprecation warnings. | `backend/app.py`, tests | low | medium | Future upgrade friction | Address during modularization | SQLAlchemy modernization | medium | false |
| TD-021 | PDF/SWIG deprecation warnings | Existing dependencies can emit warnings in test runs. | PDF/OCR dependencies | low | medium | Noise in CI | Pin/upgrade dependencies after pilot | Dependency hygiene | small | false |
| TD-022 | Single-file frontend scale | `frontend/index.html` has 3k+ lines and multiple role workspaces. | `frontend/index.html` | high | high | UI maintainability risk | Split into static JS modules/components without changing UX | Frontend modularization | large | false |
| TD-023 | Tests rely on internals | Some tests import seed scripts and inspect models directly. | `tests/*` | medium | medium | Refactor friction | Keep for pilot; add more black-box API tests later | Test strategy cleanup | medium | false |
| TD-024 | Old SQLite data may need backfill | Additive migration creates columns but cannot infer all new field values. | `scripts/migrate_db.py`, old DBs | high | medium | Missing course/feedback metadata in legacy rows | Add explicit backfill scripts before production import | Legacy backfill | medium | false |
| TD-025 | Potential N+1/full scans | Queue/analytics/search routes use simple queries that may scan as data grows. | analytics, review queue, feedback queue | medium | medium | Performance degradation | Add indexes and profiling with pilot volume data | Query optimization | medium | false |
| TD-026 | Provider policy GET allowed student before 9A | Students could read provider registry/policy summaries. | `backend/app.py` | high | medium | Governance information exposure | Fixed in Task 9A: provider list/policy GET now require teacher/admin | Completed 9A fix | small | false |
| TD-027 | Feedback upgrade path missed status/course fields before 9A | Old feedback tables did not receive all fields needed by Concept Card feedback queues. | `backend/app.py` | critical | medium | Old DB upgrade could break feedback triage/analytics | Fixed in Task 9A: additive feedback columns include status/course/chapter/term metadata | Completed 9A fix | small | false |
| TD-028 | Readiness verdict drift before 9B | The script previously printed `READY` while the report said `READY WITH CONDITIONS`. | `scripts/pilot_readiness_check.py`, `docs/pilot_readiness_report.md` | high | medium | Operators could misread pilot scope as production-ready | Fixed in Task 9B with `READY`, `READY_WITH_CONDITIONS`, `NOT_READY`, JSON output, and tests | Completed 9B fix | small | false |
| TD-029 | Browser runtime dependency in CI/pilot hosts | The current local environment has Python Playwright and Chromium installed, but every CI/pilot host still needs explicit runtime provisioning. | `requirements-e2e.txt`, `.github/workflows/browser-e2e.yml`, `docs/pilot_runbook.md` | medium | medium | Browser E2E can become unavailable on new machines if the runtime is not installed | Use the dedicated Browser E2E workflow/runbook install steps; fail readiness if available E2E fails | Browser runtime provisioning | small | false |
| TD-030 | SQLite backup consistency is operator-controlled | SQLite backup now uses the SQLite backup API, but pilots must still stop or limit writes and verify backups operationally. | `scripts/pilot_backup.py`, `docs/pilot_runbook.md` | medium | medium | Backup may not reflect intended operational checkpoint if writes continue | Follow runbook stop-writes procedure; move to managed database backup before production | Backup procedure hardening | medium | false |
| TD-031 | Production monitoring not implemented | Readiness records production monitoring as an explicit condition. | docs, deployment scripts | high | medium | Pilot incidents require manual inspection | Add structured health checks, error alerting, and audit retention after pilot | Monitoring/alerting | medium | false |
| TD-032 | Route-refactor work existed too long as an uncommitted working tree | Tasks through 9C.3B were verified locally before a reproducible Git checkpoint existed. Task 9C.3C establishes a cumulative checkpoint and clean-worktree verification instead of pretending the earlier tasks had separate commits. | Git history, route modules, tests, docs | high | medium | Reproduction, handoff, and rollback were unclear until the checkpoint | Keep using narrow checkpoint commits before further route extraction | Repository baseline hygiene | small | false |
| TD-033 | Historical release artifact archive is incomplete | The historical `lexibridge AI/` release/backup copy was quarantined outside the repository, but only 13 of the original 31 files remained when Task 9C.3C resumed. The original manifest listed 31 files and 9,850,040 bytes; the preserved durable archive contains 13 files and 9,584,911 bytes. The missing files cannot be recovered from the current environment. This archive is not current source and is not a build/runtime dependency. | `$HOME/Documents/LexiBridge-AI-Archives/route-refactor-pre-checkpoint-20260714-INCOMPLETE` | low | low | Historical backup completeness is reduced, but current source is unaffected | Keep the incomplete archive outside Git with its durable manifest; do not rely on it for current builds or tests | Historical artifact retention | small | false |
| TD-034 | Legacy healthcheck live probe echoes adapter messages | Task 9C.4L characterization showed `services.ai_health.healthcheck_provider(...)` returns adapter/provider error `message` for live probe failures. Task 9C.4L.1 mitigates the active route risk: `POST /api/admin/ai/healthcheck` now returns `LEGACY_LIVE_PROBE_DISABLED` when `live_probe=true` and does not call provider adapter or transport. Task 9C.4M moves local readiness calculation into `backend/services/legacy_provider_local_readiness.py`, which receives only safe provider snapshots and `credential_present: bool`. Task 9C.4N moves the thin HTTP adapter into `backend/routes/legacy_provider_admin_healthcheck.py` without re-enabling transport. The lower-level helper still needs explicit redaction before any future live-probe service is enabled. | `backend/app.py`, `backend/routes/legacy_provider_admin_healthcheck.py`, `backend/services/ai_health.py`, `backend/services/legacy_provider_local_readiness.py`, `scripts/pilot_readiness_check.py`, `docs/legacy_provider_healthcheck_boundary.md`, `tests/test_legacy_provider_healthcheck_characterization.py`, `tests/test_legacy_provider_live_probe_disabled.py`, `tests/test_legacy_provider_local_readiness_service.py` | high | low | Legacy endpoint no longer exposes the live transport path, but a future service could reintroduce unsafe raw message passthrough if not designed carefully | Keep legacy live probe disabled; only add a future live-probe service with explicit opt-in, timeout, transport spy tests, and redaction | Healthcheck safety boundary | medium | false |
| TD-035 | Legacy prompt mutation policy is under-specified | Task 9C.4O characterization showed `POST /api/admin/ai/prompts` is a legacy upsert keyed by `prompt_key` + `prompt_version`. Updates are in-place, versions are client-supplied, active/default flags are assigned directly, no explicit uniqueness or optimistic-locking policy exists, OpenAPI required fields are stricter than the implementation, and the callback commits without an explicit rollback. | `backend/app.py`, `backend/routes/legacy_provider_admin_configuration.py`, `docs/legacy_provider_prompt_mutation_boundary.md`, `tests/test_legacy_provider_prompt_mutation_characterization.py` | high | medium | Direct route extraction could freeze ambiguous version/default/concurrency behavior or copy weak rollback semantics into a new service | Define prompt mutation validation, version/default, conflict/concurrency, and transaction policy before service extraction | Prompt mutation policy boundary | medium | false |

## Current Pilot-Blocking Debt

No unresolved `blocking_pilot=true` debt is listed after the Task 9B.1 hardening. The highest remaining risks are operational and production-readiness risks, not blockers for a controlled local or small-course pilot. The current readiness verdict remains `READY_WITH_CONDITIONS` because SQLite, Flask dev server, disabled external LLM providers, non-formal migrations, and missing production monitoring are accepted only for the bounded pilot profile.

## Staged Route Extraction Plan

Task 9C.1 started mitigation for TD-001 by extracting only the teacher learning analytics route group. Task 9C.2 continued the same pattern by extracting only the student Concept Card learning route group. Task 9C.2.1 added `RouteCoreDependencies` in `backend/routes/shared.py` so extracted modules share common route infrastructure without hiding domain services in a service locator. Task 9C.3A extracted only the teacher Concept Card review route group. Task 9C.3B extracted only the teacher-facing student feedback queue and triage route group. Task 9C.3C establishes the cumulative route-refactor checkpoint and records the clean-worktree verification requirement in `docs/route_extraction_checkpoint.md`. Task 9C.4A extracts only read-only provider governance/preflight GET routes and leaves provider policy mutation, preflight execution, alignment verification execution, provider usage writes, and external transports in `backend/app.py`/services. Task 9C.4B extracts only provider policy mutation and leaves preflight execution, alignment verification execution, provider usage writes, replay execution, and external transports in `backend/app.py`/services. Task 9C.4C extracts only provider preflight execution and leaves alignment verification execution, provider usage writes, replay execution, credential management, and external transports in `backend/app.py`/services. Task 9C.4D freezes the `/api/alignment/verify` route boundary and records that direct route extraction should wait for a service boundary because the handler still coordinates provider execution, usage, attach, audit, and transaction behavior. Task 9C.4D.1 establishes that service boundary in `backend/services/alignment_verification_execution.py`; Task 9C.4E moves only the thin HTTP adapter into `backend/routes/alignment_verification.py`; Task 9C.4F inventories remaining provider/admin routes in `docs/provider_admin_route_inventory.md` without moving production code; Task 9C.4G extracts only `GET /api/admin/alignment-runs`; Task 9C.4I extracts only legacy provider admin observability GETs; Task 9C.4J moves legacy provider registry seed and flush behavior into `backend/services/legacy_provider_registry_seed.py` without migrating another route or changing transaction ownership; Task 9C.4K extracts only seed-backed provider/model/prompt configuration GETs. Follow-up slices should remain similarly narrow:

- 9C.4H: legacy `/api/admin/ai/*` audit completed. 9C.4I extracted the safe observability GET views. 9C.4J centralized seed service behavior while preserving GET flush and POST commit semantics. 9C.4K extracted the seed-backed provider/model/prompt GET views. 9C.4L characterized `POST /api/admin/ai/healthcheck`; 9C.4L.1 disabled legacy live probe transport and added readiness coverage; 9C.4M established the local readiness service; 9C.4N extracted the thin healthcheck route adapter. 9C.4O characterized prompt mutation and concluded `PROMPT_VERSIONING_OR_CONCURRENCY_POLICY_REQUIRED_FIRST`; legacy `/api/alignment/run` remains a separate task.
- 9C.5: continue upload/knowledge route extraction in narrow slices after provider/admin boundaries are stable.
- App factory and shared route utilities should wait until route contracts are stable.

Do not mark TD-001 fully resolved until model declarations, migrations, route registration, and shared helpers are separated behind a tested app factory or equivalent structure. Provider execution/mutation, upload/knowledge/evidence routes, course/policy management routes, and other legacy route domains still live in `backend/app.py`; Blueprint unification, a dependency container, and RBAC helper consolidation have not started.
