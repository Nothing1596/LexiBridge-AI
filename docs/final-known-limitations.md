# Final Known Limitations

Canonical copy: `final_delivery/final_known_limitations.md`.

The current project is local pilot-ready, not production-ready. SQLite, LocalStorageBackend, local worker, mock payment, mock email, local hash embedding, and synthetic demo data are not production capabilities.
